<!-- 「始终注入」层：放必须每轮都生效、且容易被长上下文/英文技术内容带跑的硬指令——典型是回复语言与语气。
     全局 hook inject-conventions.sh 在【每轮】UserPromptSubmit 都注入本文件（不依赖编码关键词、不依赖 jq），
     而完整编码规范只在编码类 prompt 注入。本文件尽量短（每轮都推，省 token）。
     这是【你的个人文件】——rig 安装不覆盖、回退不删除。删掉本注释块、按需填写。 -->

## 交流语言
<填你的偏好。例：始终用简体中文回复（叙述/解释/分析/结论）；代码、命令、文件名、API/类名、专有名词可保留原文。即使上下文大量是英文或技术术语，也不切换语言。>
