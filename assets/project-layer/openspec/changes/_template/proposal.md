# <用一句话概括这次变更：要做成什么>

> openspec change 提案。前向规格——**先说清要建什么、为什么，再动手**。
> 配套 `tasks.md`（拆解执行清单）与 `specs/<域>/spec.md`（spec-delta，用 `## ADDED/MODIFIED/REMOVED Requirements`）。
> 三件套齐全后跑 `openspec validate <change>`；变更落地、tasks 全勾选后跑 `openspec archive <change>` 合并进 `openspec/specs/`。
> 填写时删掉所有 `<尖括号占位>` 和本引用块。

## 背景

<为什么现在要做这个变更。当前是什么状态、痛在哪、不做会怎样。给出触发动机（用户诉求 / 线上问题 / 技术债 / 新依赖），让读者无需追问就理解必要性。只讲事实，不写空泛口号。>

## 目标

<这次变更要达成的可验证结果，逐条列。每条应能被 tasks 或验收命令检验。>

- <目标 1：例如「新增 X 能力，用户可通过 Y 完成 Z」>
- <目标 2>

**非目标**（明确不做，防范围蔓延）：

- <本次不解决的相关问题，留待后续或永不做的原因>

## 范围

<划清边界：改哪些、不动哪些。让执行者和评审一眼看清影响面。>

- **改动**：<涉及的模块 / 目录 / 接口 / 数据结构>
- **不动**：<明确保持现状的部分，尤其是容易被顺手改的相邻代码>
- **影响面**：<对外契约 / 数据迁移 / 兼容性 / 依赖方，有则列，无则写「无」>

## 关键设计取舍

<本次的主要技术决策与权衡：为什么选 A 不选 B。只记真正有分歧、需要解释的点，显而易见的不写。>

- <取舍 1：在「<方案 A>」与「<方案 B>」之间选 A，因为 <理由 / 约束 / 代价>>
- <取舍 2>

> 跨域、长期生效、影响后续多个变更的决策，落地后应**毕业到 ADR**（`docs/adr/`）作为「为什么」的唯一权威；这里只留本次变更的局部权衡。

## 风险与回滚

<可能出错的地方与应对。让评审能判断风险是否可接受。>

- **风险**：<潜在问题，如数据不兼容 / 性能退化 / 破坏既有行为> → **缓解**：<如何降低或监测>
- **回滚**：<出问题时如何撤回。是否可直接 revert、是否有数据迁移需逆向、是否需开关位灰度>
