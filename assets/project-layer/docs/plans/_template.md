# 计划：<一句话目标>

> superpowers plan 阶段产出的骨架；执行前先把每一节填实，尤其「验收标准」必须可执行。
> 这份计划是 L4 完成度度量的对照基准——验收标准写得越硬，「做完没做完」越没法含糊。

## 目标
<这次改动要达成的最终状态，1-3 句。只说做完后世界什么样，不展开方案。>

## 范围
- 改：<会动到的模块/能力，逐条列>
- 不改：<明确划在范围外的部分，防止顺手扩张>
- 假设/前置：<依赖的外部条件、已确认的约定；不确定的先去澄清，别写进来当事实>

## 步骤
> 有序、可串行执行；每步都该是一次可独立提交的小改动。
1. <步骤一：动词开头，说清改哪个文件/函数、改成什么>
2. <步骤二>
3. <步骤三>

## 验收标准
> 每条都必须是「能跑出来的命令 / 测试断言」，而非"实现了登录"这类无法度量的描述。
> 写不出可执行判据的条目，说明目标还没想清——回去拆。
- [ ] `<可执行命令>` 退出码为 0，例如 `bash scripts/verify-local.sh`
- [ ] 新增测试 `<测试文件::用例名>` 通过，断言 `<具体输入 → 期望输出>`
- [ ] `<curl/CLI 调用>` 返回 `<具体状态码/字段值>`，例如 `curl -s localhost:8080/health` 返回 `{"status":"UP"}`
- [ ] 回归：`<原有测试/lint 命令>` 仍全绿
- [ ] <若有契约改动> openspec change `<change-id>` 的 tasks 全部勾完且 `openspec validate` 通过

## 受影响文件
> 列出预计新增/修改/删除的文件，供 review 和回滚对照。
- 新增：<path>
- 修改：<path>
- 删除：<path>

## 回滚方案
- 触发条件：<什么信号表明这次改动要撤，如验收某条红、线上指标异常>
- 回滚动作：<具体怎么撤——`git revert <commit/PR>`；是否需迁移回退、特性开关关闭、缓存/数据清理等>
- 影响面：<回滚后受影响的功能与用户，需通知谁>
