# 结构约定（模板 — 按本项目实际填写）

> 删掉示例占位，换成本项目真实规则。结构/依赖类规则尽量做成架构测试（A 桶）。
> 初始化时可参考 rig 内置的项目类型/语言候选模板，但模块边界、目录职责和依赖规则必须来自项目现状、已有规范或用户确认。

## 模块放置  [A 桶 → 架构测试]
- <哪类代码放哪个模块>
- <非编码资料项目写目录职责、资料来源、产物位置和归档规则>

## 包结构  [A 桶]
- <服务内固定分层骨架；entity 子包；可选层>
- <轻量脚本/demo 可简化为 bin/scripts/src/tests/docs 等最小结构>

## 归属判断  [B 桶 → 注入 + review]
- <只服务一个域 → 域内；跨域 → 上移；跨服务 → 下沉公共；判断依据是"实际调用面">
- <混合项目要区分源码、资料、配置、生成物，不能互相偷放>

## 模块依赖矩阵  [A 桶 → 架构测试，最值得编译成测试]
- <模块 A 不许依赖模块 B …… 直接写成 ArchUnit/dependency-cruiser 规则>
- <暂时无法机器检查的依赖边界，先写成 B 桶规则，并在 review 收尾核对>
