# 编码约定（模板 — 按本项目实际填写）

> 删掉示例占位，换成本项目真实规则。每条尽量标注属于 A/B/C 哪一桶（见 README）。
> 初始化时可参考 rig 内置的项目类型/语言候选模板，但只能保留本项目已有规范、代码和用户确认后真实适用的条目。

## 命名与模型  [多为 A 桶 → 可 lint]
- <Controller/Service/DAO/实体/入参/返回 的命名约定>
- <若是非编码资料项目，改写为文档、资料、目录、版本号的命名约定>

## 分层职责  [A+B 桶]
- <每层做什么、不做什么；跨层调用规则>
- <若是轻量脚本/demo，写清入口、输入、输出、配置和风险边界即可>

## 注释、日志与异常  [存在性=A 桶可 lint；信息量=B 桶靠注入+review]
- <注释风格、类/方法/字段注释要求、日志与异常语言>
- <资料项目改写为事实来源、待确认标注、引用路径和输出口径>

## 代码风格细则  [多为 A 桶]
- <禁魔法值、注入方式、常量沉淀等>
- <按语言选择 Java/TypeScript/Python/Go 等检查器，不要把无关语言规则混进来>

## 禁止做的事  [尽量做成 A 桶 lint]
- <明确禁止项清单>
- <受保护文件、生产数据、密钥、外部服务调用等项目红线>
